package com.example.forgegen

object AppVersion {
    /** Shown on the start screen, e.g. "v1.0.0" (debug builds: "v1.0.0-DEBUG"). */
    val currentVersion: String = "v${BuildConfig.VERSION_NAME}"
}
