<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the downloaded update APK (getExternalFilesDir(DIRECTORY_DOWNLOADS)) is shared, with the package installer. -->
    <external-files-path name="download_files" path="Download/" />
</paths>
